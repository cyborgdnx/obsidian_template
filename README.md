# obsidian_template
This repo contains the template that could be used for obidian note taking in the future

This template can be directly used in any other system. It has to be ensured that the following changes are made.

Currently, the template is being named as `template`. When putting it in a vault, the one should rename it to `.obsidian`. This will automatically make the necessary changes
